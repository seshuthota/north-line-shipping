# OpenRouter key — kept on the server. Never expose this in client code.
# Create a key at https://openrouter.ai/keys then restart `npm run dev`.
OPENROUTER_API_KEY=

# Turso shipping-data database — server-side only.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
